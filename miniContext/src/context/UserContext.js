import React from 'react'

const UserContext = React.createContext();
export default UserContext;

// context gives provider
// wrap components with it, as a wrapper
// those components get access to all states 

